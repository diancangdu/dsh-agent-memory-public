# 全局指令 · 长期记忆（模板）

> 这是一份可直接使用的模板。把 `<尖括号>` 占位符换成你自己的值即可。
>
> 这个文件在每个会话的第一次请求时被自动注入（有 64 KiB 预算）。
> 它同时是「给 agent 的指令」和「跨会话的长期记忆本体」——
> 改它即刻对后续会话生效，**已经进行中的会话不会被重新注入**。

---

## 一、记忆机制

### 分层

| 层 | 位置 | 加载方式 | 放什么 |
|---|---|---|---|
| 用户级 | `<DSH_HOME>/AGENTS.md`（本文件） | **自动注入**，所有项目 | 跨项目的硬规则、环境坑 |
| 项目级 | `<项目根>/AGENTS.md` | **自动注入**（会扫项目根到工作目录每一层） | 该项目的约定、结构、技术选型 |
| 项目日志 | `<项目根>/memory/YYYY-MM-DD.md` | 按需读、**必须写** | 逐轮做了什么的流水 |
| 历史会话 | — | `session_search` 主动检索 | 更早对话的细节 |

### 写入规则（满足任一就**直接写，不要问用户**）

- 用户说「记住」「以后都这样」「别再…」
- 用户纠正了你的判断或做法
- 确立了项目约定 / 技术选型 / 目录结构 / 命名规范
- 解决了非显而易见的问题（尤其排查超过 10 分钟的）

**写到哪**：

- 跨项目通用 → 本文件第三节
- 某个项目的 → 该项目的 `AGENTS.md`（记忆本体）+ `memory/YYYY-MM-DD.md`（当轮日志）

**怎么写**：

- 记忆本体：一行一条，`- 事实。判据或原因。（YYYY-MM-DD）`。**只记结论，不记过程。**
- 日志：**append-only**，写「做了什么 / 判据 / 还剩什么没做」，不删旧内容
- 同一件事有更新 → **改写原来那条**，不要追加第二条

### 自学习闭环

这条不依赖任何每轮提醒，**它一直在生效**：

- **任务前**：先查经验库，命中就照做。
- **任务后**：只要本轮**用过工具**，就必须做一次反思判定（什么有效 / 什么失败 / 有没有该固化的模式）。
  **判定动作本身不得跳过**；判定为无可复用内容时允许不记录，但**禁止凑数写噪音**。
- **记录**：按类型分流（正向经验 / 失败教训 / 高层准则）。
- **边界**：单点知识先记成经验条目；同一流程反复出现才升级为 Skill。

### 维护规则（必须遵守，否则记忆会烂掉）

- **本文件**：每个小节 ≤ 15 条，超了就归并同类、删掉过期项。
  它每次会话都注入，写得越长越浪费上下文 —— **记忆的价值在密度，不在长度**。
- **项目 `AGENTS.md`**：≤ 200 行。超出时把细节挪到 `memory/MEMORY_detail.md`，
  正文只留硬约束 + 指向细节的索引。
- **日志**：超过 30 天就按主题归并进项目 `AGENTS.md`，然后删掉旧日志文件。

### 会话开场自检

新会话开始时，如果工作在某个项目里，先确认该项目根有 `AGENTS.md`；
没有就按上面的分层建一个（含 `memory/` 目录），把已知信息填进去。

---

## 二、环境与硬约束

> 放**客观事实**：这台机器/这套环境是什么样，与本文件之外无关，按事实执行即可。

- `<数据盘>:\<你的数据区>` 是主数据区；大文件、缓存、模型权重一律放这里，不落系统盘。
- 缺工具先在本机找，本地有就直接用，**不要过度安装**。
- 本机 shell 是 `<你的 shell 类型>`：裸命令可能缺失，优先用专用工具；
  长命令输出要落盘再读。
- **破坏性操作前**必须先列清单、说明风险，拿到明确确认再动手。绝不递归删个人目录。
- 验证类工作要给**可复现的判据**（读数、测试结果），不要给形容词。

### 常见环境坑（示例，替换成你自己踩过的）

- 某些下载工具在受管网络下会因证书吊销检查失败而**静默不落盘** —— 需要显式跳过该检查。
- 目录可能是 **junction / symlink**：统计「某个东西是否在系统盘」时**必须先识别重解析点**，
  否则会把其它盘的实体误报成系统盘占用。
  **判据**：`os.lstat()` 的 `st_file_attributes & FILE_ATTRIBUTE_REPARSE_POINT`。
  注意 `os.path.islink()` 对 junction 返回 **False**，别拿它判断。
- 写 PowerShell 脚本时 `.ps1` 必须是 **ASCII**：无 BOM 的 UTF-8 会被按本地代码页读，
  中文注释会吃掉引号导致解析失败，而 stdout 全空、看着像被执行策略拦了。
- 变量名大小写不敏感：`$NEW` 与 `$new` 是同一个变量，循环里赋值会静默覆盖。

---

## 三、长期记忆

> 这一节由你自己积累。下面是**条目格式的示例**，不是真实内容。

### 用户偏好

- 用户偏好**结论先行**，不要复述过程；给判据而不是形容词。（YYYY-MM-DD）
- 用户要求反思类动作**保持静默**，不要把判断过程输出到回复里。（YYYY-MM-DD）

### 项目

- `<项目名>`：`<一句话定位>`；约定见该项目的 `AGENTS.md`。（YYYY-MM-DD）

### 本机环境

- `<DSH_HOME>` 是指向 `<数据盘>:\<路径>` 的 junction，所以从系统盘路径读到的大小是穿透后的真实占用，
  但路径本身只是链接。（YYYY-MM-DD）

### 踩过的坑

- 某配置文件改动**只在进程启动时读一次**，没有热重载 —— 改完必须重启才生效。（YYYY-MM-DD）
- 某插件机制在「应用阶段拿不到服务」时会**静默不激活**：装上了、也在配置树里，
  但什么都不做。判断是否真在起作用，要找**独立于该路径的旁证**（日志、副作用），
  不能拿「配置里有它」当成功判据。（YYYY-MM-DD）
