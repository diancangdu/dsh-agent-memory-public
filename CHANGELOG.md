# Changelog

## v1.0.0 — 2026-09-25

首个公开版本。

**性质**：这个仓库是**模板与说明**，不含任何可直接生效的内容 —— 生效靠你填进去的东西。
原样（含本机信息）的版本另存于 private 版仓库。

### 提供什么

- `AGENTS.template.md` —— 分层长期记忆模板：机制（分层 / 写入规则 / 维护规则 / 开场自检）
  完整保留，机器细节换成 `<尖括号>` 占位符，并附**条目格式的示例**（不是真实内容）。
- `README.md` —— 为什么必须分层（注入有预算、且每次都注入）、三个最容易被做错的地方、
  以及与「经验库」的分工。
- `install.ps1` —— 安装器，**默认什么都不破坏**。
- `INSTALL.md` —— 自动化与手动两条安装路线，含验证判据与卸载。

### 安装器最重要的一条性质

**它拒绝覆盖已存在的 `AGENTS.md`。** 已存在的记忆是用户积累的知识，覆盖它等于毁掉记忆。
命中已存在文件时脚本**停止并退出**，只有显式 `-Force` 才会替换，且替换前留时间戳备份。

### 三个最容易被做错的地方（README 里也写了）

1. **只记结论，不记过程。** 条目写成 `- 事实。判据或原因。（日期）`。记忆本体每次会话都注入，
   日志不注入 —— 把过程写进本体等于每次都浪费预算。
2. **同一件事有更新就改写原条目，不要追加第二条。** 否则同一事实出现多个互相矛盾的版本，
   而 agent 无从判断哪个最新。
3. **「记住了」和「真的在起作用」是两件事。** 文件里有它 ≠ 它被注入 ≠ agent 照做。
   三个强度依次递增，验证必须走到第三层（写一条**可验证的**约束，看它是否遵守）。

### 体量约束（不是建议，是机制要求）

- 用户级：每个小节 ≤ 15 条（它每次会话都注入，越长越浪费上下文）
- 项目级：≤ 200 行，超出时把细节挪到 `memory/MEMORY_detail.md`，正文只留硬约束 + 索引
- 日志：超过 30 天按主题归并进项目 `AGENTS.md`，然后删掉旧日志

### 已知边界

- 自动注入只发生在**每个会话的第一次请求**：已经进行中的会话不会被重新注入，需要新开会话
- 各宿主对「项目根到工作目录每一层」的扫描顺序可能不同，跨层同名文件以更近的为准
- 文件名必须是 `AGENTS.md`、位置必须是约定路径 —— 换个名字或目录就不会被加载
- 本仓库**不含任何可直接生效的内容**；生效靠你填写的占位符
