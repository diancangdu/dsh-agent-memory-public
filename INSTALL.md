# 安装教程

这个仓库是**模板**，不是可直接生效的内容。装完必须填占位符才会起作用。

两条路：**自动化脚本**（推荐，带防误删保护）或**手动**。

---

## 前置条件

- 无依赖，纯文件
- 只需要知道两件事：`<DSH_HOME>` 在哪、你的项目根在哪

> 文件名必须是 `AGENTS.md`，位置必须是约定路径 —— 宿主是按**约定**自动扫描注入的，
> 换个名字或换个目录就不会被加载。

---

## 一、自动化安装

```powershell
# 干跑：看看会写哪些文件
.\install.ps1 -DshHome "$env:DSH_HOME" -ProjectRoot D:\myproject

# 确认后写入
.\install.ps1 -DshHome "$env:DSH_HOME" -ProjectRoot D:\myproject -Apply
```

参数：

| 参数 | 说明 |
|---|---|
| `-DshHome` | 用户级记忆目录，默认 `$env:DSH_HOME`，否则 `~/.dsh` |
| `-ProjectRoot` | 项目根；不给就只装用户级 |
| `-Apply` | 不加就是干跑 |
| `-Force` | 允许覆盖**已存在**的 `AGENTS.md`（会先留 `.bak_<时间戳>`） |

**这个脚本最重要的性质是它默认什么都不破坏**：

- 如果目标 `AGENTS.md` **已存在**，脚本**拒绝覆盖并退出**，然后告诉你怎么办
  —— 已存在的记忆是你积累的知识，覆盖它等于毁掉记忆
- 只有显式 `-Force` 才会替换，且替换前留时间戳备份

装完还要做一步（脚本会提示）：把每个 `<尖括号>` 占位符换成你自己的值，并删掉示例条目。

---

## 二、手动安装

### 1. 用户级记忆

```powershell
Copy-Item .\AGENTS.template.md "$env:DSH_HOME\AGENTS.md"
```

这一份**所有项目都会注入**，所以只放跨项目的硬规则与环境坑，每小节 ≤ 15 条。

### 2. 项目级记忆

```powershell
Copy-Item .\AGENTS.template.md "D:\myproject\AGENTS.md"
New-Item -ItemType Directory -Force -Path "D:\myproject\memory"
```

项目级可以具体得多：该项目的目录结构、技术选型、构建与测试命令。约束是 ≤ 200 行，
超了就把细节挪到 `memory/MEMORY_detail.md`，正文只留硬约束 + 索引。

### 3. 替换占位符

打开两份 `AGENTS.md`，把所有 `<尖括号>` 换成真实值，然后删掉「示例」性质的条目。
**留着占位符不填，等于让 agent 读一份空指令。**

### 4. 开始写日志

从今天起，每轮工作往 `memory/YYYY-MM-DD.md` 追加一条：

```markdown
## 14:30 修好了登录超时
- 做了什么：把超时从 5s 提到 30s
- 判据：连续 50 次请求无超时（脚本 `scripts/probe_timeout.py`）
- 还剩：没测并发场景
```

**append-only，不删旧内容。** 超过 30 天按主题归并进项目 `AGENTS.md`，然后删掉旧日志。

---

## 三、验证

装完**先验证它真的被注入了**，而不是「文件放对了」。

| 判据 | 怎么测 |
|---|---|
| 用户级被加载 | 新开一个会话，问它「你的长期记忆里有什么跨项目约束」，应能说出你写进去的内容 |
| 项目级被加载 | 在项目目录里新开会话，问它该项目的构建命令，应能答出 |
| 记忆真的生效 | 写一条**可验证的**约束（如「本仓库提交信息用中文」），然后让它做一次提交，看是否遵守 |

⚠️ **「文件存在」不等于「内容被注入」**，也不等于「agent 照做」——
这是三个不同强度的判据。上面第三行才是真正的行为判据。

**改了没生效？** 自动注入只发生在**每个会话的第一次请求**：
已经进行中的会话不会被重新注入，需要新开会话。

---

## 四、卸载

```powershell
# 用户级
Remove-Item "$env:DSH_HOME\AGENTS.md"
# 项目级
Remove-Item "D:\myproject\AGENTS.md"
Remove-Item -Recurse "D:\myproject\memory"
```

⚠️ 删之前**先备份** —— 这是你积累的记忆，不是可再生的配置。
建议纳入 git（项目级那份尤其适合跟代码一起版本化）。

---

## 五、常见问题

| 现象 | 原因 | 处理 |
|---|---|---|
| 写进去了但 agent 不知道 | 注入只发生在会话的第一次请求 | 新开会话 |
| 内容太长，重要的被淹没 | 没有遵守体量约束 | 用户级每小节 ≤ 15 条；项目级 ≤ 200 行 |
| 同一件事有两三条互相矛盾 | 更新时新加了条目而没改写旧的 | **改写原条目**，不要追加 |
| 项目级和用户级冲突 | 两层都在注入 | 越靠近工作目录的层级优先级越高；冲突说明该归并了 |
| 日志越写越多 | 没有归并 | 超 30 天按主题并进项目 `AGENTS.md`，删旧日志 |
